# Target corpus acquisition outputs

Folder này sẽ chứa trạng thái targeted crawl cho MIT 6.0001.

Output dự kiến:

```text
acquisition_status.csv
acquisition_summary.csv
```

Không lưu raw transcript trong `reports/`. Raw payload vẫn thuộc Bronze layer.
